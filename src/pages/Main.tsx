import React from 'react';

import { UserHeader } from '../components/UserHeader';

export const Main: React.FC = () => {
    return (
        <div className="main">
            <UserHeader />
        </div>
    );
};

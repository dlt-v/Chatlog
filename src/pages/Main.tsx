import React from 'react';

import { Navigation } from '../components/Navigation';

export const Main: React.FC = () => {
    return (
        <div className="main">
            <Navigation />
        </div>
    );
};